---
// Header component con navegación y dark mode toggle
import { Menu, PieChart } from "lucide-react";
---

<header
  class="sticky top-0 z-50 glass-header border-b border-solid border-[#e7eaf3] dark:border-gray-800 px-6 md:px-10 py-3"
>
  <div
    class="max-w-[1280px] mx-auto flex items-center justify-between whitespace-nowrap"
  >
    <!-- Logo -->
    <div class="flex items-center gap-3">
      <div class="size-8 text-primary">
        <PieChart size={32} />
      </div>
      <h2
        class="text-primary dark:text-teal-400 text-xl font-black leading-tight tracking-tight"
      >
        MDA Analítica
      </h2>
    </div>

    <!-- Navigation -->
    <nav class="hidden lg:flex flex-1 justify-center gap-9">
      <a
        class="nav-link text-sm font-semibold leading-normal relative py-1"
        href="/#inicio">Inicio</a
      >
      <a
        class="nav-link text-sm font-semibold leading-normal relative py-1"
        href="#soluciones">Soluciones</a
      >
      <a
        class="nav-link text-sm font-semibold leading-normal relative py-1"
        href="#analitica">Analítica</a
      >
      <!-- <a
        class="nav-link text-sm font-semibold leading-normal relative py-1"
        href="#blog">Blog</a
      > -->
      <a
        class="nav-link text-sm font-semibold leading-normal relative py-1"
        href="#nosotros">Nosotros</a
      >
    </nav>

    <!-- Actions -->
    <div class="flex gap-3 items-center">
      <button
        onclick="document.getElementById('rif-modal').classList.remove('hidden')"
        class="flex min-w-[160px] cursor-pointer items-center justify-center overflow-hidden rounded-lg h-10 px-6 bg-accent-green text-white text-sm font-bold leading-normal tracking-wide transition-all hover:bg-emerald-600 border border-emerald-400/20 glow-accent"
      >
        <span>Ir a RIF-Analytic</span>
      </button>
    </div>

    <!-- Mobile menu button -->
    <button
      class="lg:hidden flex items-center justify-center rounded-lg h-10 w-10 text-gray-600 dark:text-gray-300"
      id="mobile-menu-btn"
    >
      <Menu size={24} />
    </button>
  </div>
</header>

<style>
  /* Base style for link text */
  .nav-link {
    color: #4b5563; /* gray-600 */
    transition: color 0.3s ease;
  }

  :global(.dark) .nav-link {
    color: #9ca3af; /* gray-400 */
  }

  .nav-link:hover {
    color: #007878; /* primary approx */
  }

  :global(.dark) .nav-link:hover {
    color: #2dd4bf; /* teal-400 approx */
  }

  /* Active state text color */
  .nav-link.active-link {
    color: #007878; /* primary */
  }

  :global(.dark) .nav-link.active-link {
    color: #2dd4bf; /* teal-400 */
  }

  /* Underline animation using pseudo-element */
  .nav-link::after {
    content: "";
    position: absolute;
    bottom: -4px; /* Adjust distance from text */
    left: 0;
    width: 0;
    height: 2px;
    background-color: #007878; /* primary */
    transition: width 0.3s ease-in-out;
  }

  :global(.dark) .nav-link::after {
    background-color: #2dd4bf; /* teal-400 */
  }

  /* Show underline on active or hover if desired, usually just active for 'traveling' */
  .nav-link.active-link::after {
    width: 100%;
  }
</style>

<script>
  // Active Link Scroll Spy Logic
  document.addEventListener("DOMContentLoaded", () => {
    const sections = ["inicio", "soluciones", "analitica", "blog", "nosotros"];
    const navLinks = document.querySelectorAll(".nav-link");

    const observerOptions = {
      root: null,
      rootMargin: "0px",
      threshold: 0.3, // Trigger when 30% of section is visible
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          // Remove active class from all links
          navLinks.forEach((link) => link.classList.remove("active-link"));

          // Add active class to corresponding link
          const activeId = entry.target.id;
          const activeLink = document.querySelector(
            `.nav-link[href*="#${activeId}"]`,
          );
          if (activeLink) {
            activeLink.classList.add("active-link");
          }
        }
      });
    }, observerOptions);

    sections.forEach((id) => {
      const section = document.getElementById(id);
      if (section) {
        observer.observe(section);
      }
    });
  });
</script>
